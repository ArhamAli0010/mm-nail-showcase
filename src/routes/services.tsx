import { createFileRoute,Link } from "@tanstack/react-router";import { Clock } from "lucide-react";import { Button } from "@/components/ui/button";import { PageHero,SectionIntro } from "@/components/studio/Parts";import { imagery,services } from "@/lib/studio-data";
export const Route=createFileRoute("/services")({head:()=>({meta:[{title:"Nail Services — MM Nail Studio"},{name:"description",content:"Explore the complete placeholder treatment menu at MM Nail Studio."},{property:"og:title",content:"Nail Services — MM Nail Studio"},{property:"og:description",content:"Considered manicures, extensions and nail art in Newbury."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary_large_image"}]}),component:Services});
function Services(){return <><PageHero eyebrow="Studio services" title="The treatment menu" copy="Meticulous preparation, considered structure and polished finishes. All prices shown are presentation placeholders." image={imagery.blush}/><section className="mx-auto max-w-5xl px-5 py-24 lg:px-10"><SectionIntro eyebrow="Choose your finish" title="Made for real hands, real lives."/>{["Manicures","Extensions","Nail Art","Add-ons"].map(cat=><div key={cat} className="mt-16"><h2 className="border-b border-gold pb-4 text-4xl">{cat}</h2>{services.filter(s=>s.category===cat).map(s=><article key={s.name} className="grid gap-5 border-b border-gold/40 py-8 md:grid-cols-[1fr_auto] md:items-center"><div><h3 className="text-3xl">{s.name}</h3><p className="mt-3 max-w-2xl text-muted-foreground">{s.includes}</p><p className="mt-3 flex items-center gap-2 text-sm"><Clock className="size-4 text-gold"/>{s.duration}</p></div><div className="flex items-center gap-5 md:flex-col md:items-end"><strong className="text-primary">{s.price}</strong><Button asChild size="sm"><Link to="/booking">Book</Link></Button></div></article>)}</div>)}</section></>}
