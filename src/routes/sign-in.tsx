import { createFileRoute,Link } from "@tanstack/react-router";import { useState } from "react";import { Check } from "lucide-react";import { Button } from "@/components/ui/button";import { DemoNote } from "@/components/studio/Parts";
export const Route=createFileRoute("/sign-in")({head:()=>({meta:[{title:"Student Sign In — MM Nail Academy"},{name:"description",content:"Preview the MM Nail Academy student sign-in experience."},{property:"og:title",content:"Student Sign In — MM Nail Academy"},{property:"og:description",content:"A presentation-only student sign-in experience."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary_large_image"}]}),component:SignIn});
function SignIn(){const[mode,setMode]=useState<"login"|"reset"|"success">("login");return <section className="grid min-h-[75vh] place-items-center px-5 py-16"><div className="w-full max-w-md border border-gold bg-background p-8"><p className="eyebrow">MM Nail Academy</p>{mode==="success"?<div className="py-8 text-center"><Check className="mx-auto size-10 text-primary"/><h1 className="mt-5 text-4xl">Demo success</h1><p className="mt-4 text-muted-foreground">No account was accessed and no email was sent.</p><Button asChild className="mt-7"><Link to="/dashboard">Preview dashboard</Link></Button></div>:<><h1 className="mt-4 text-5xl">{mode==="login"?"Welcome back":"Reset access"}</h1><p className="mt-4 text-muted-foreground">{mode==="login"?"Continue your learning journey.":"Preview the password reset experience."}</p><form className="mt-8 grid gap-5" onSubmit={e=>{e.preventDefault();setMode("success")}}><label className="grid gap-2 text-sm">Email<input type="email" className="h-12 border border-gold bg-transparent px-4" placeholder="student@example.com"/></label>{mode==="login"&&<label className="grid gap-2 text-sm">Password<input type="password" className="h-12 border border-gold bg-transparent px-4" placeholder="••••••••"/></label>}<Button type="submit">{mode==="login"?"Sign in — demo":"Send reset — demo"}</Button></form><button className="mt-5 text-sm text-primary underline" onClick={()=>setMode(mode==="login"?"reset":"login")}>{mode==="login"?"Forgot password?":"Back to sign in"}</button><div className="mt-8"><DemoNote/></div></>}</div></section>}
